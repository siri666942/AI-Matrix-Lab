// 类型定义（占位文件）
// 本应用不需要复杂的类型定义

export interface User {
  id: string;
  email?: string;
  username?: string;
}

export interface Profile {
  id: string;
  username?: string;
  email?: string;
  avatar_url?: string;
  created_at?: string;
}
